#ifndef ENGINE_H
#define ENGINE_H

#include "move.h"
#include "position.h"
#include "move_generator.h"
#include <random>
#include <sstream>
#include <thread>
#include <mutex>

#define FIELD_WHITE(sq) (64 - sq - 1)
#define FIELD_BLACK(sq) ((sq / 8) * 8 + 8 - sq % 8 - 1)

class Engine
{
    Move moves[64][256];
    MoveGenerator generator;
    Position position;
    std::thread thread;
    std::mutex mutex;
    bool goBool = true;
    std::chrono::milliseconds time;
    std::chrono::steady_clock::time_point begin;
    std::chrono::steady_clock::time_point end;
    Move stored_best_move;
    int stored_eval;
    bool initialized = false;
    int nodes = 0;
    int search_depth;
    int seldepth = 0;
    int quiescent_nodes = 0;
    // int nodes_pruned = 0;

    int pawns[64] = {
        0, 0, 0, 0, 0, 0, 0, 0,
        50, 50, 50, 50, 50, 50, 50, 50,
        10, 10, 20, 30, 30, 20, 10, 10,
        5, 5, 10, 25, 25, 10, 5, 5,
        0, 0, 0, 20, 20, 0, 0, 0,
        5, -5, -10, 0, 0, -10, -5, 5,
        5, 10, 10, -20, -20, 10, 10, 5,
        0, 0, 0, 0, 0, 0, 0, 0};

    int knights[64] = {
        -50, -40, -30, -30, -30, -30, -40, -50,
        -40, -20, 0, 0, 0, 0, -20, -40,
        -30, 0, 10, 15, 15, 10, 0, -30,
        -30, 5, 15, 20, 20, 15, 5, -30,
        -30, 0, 15, 20, 20, 15, 0, -30,
        -30, 5, 10, 15, 15, 10, 5, -30,
        -40, -20, 0, 5, 5, 0, -20, -40,
        -50, -40, -30, -30, -30, -30, -40, -50};

    int bishops[64] = {
        -20, -10, -10, -10, -10, -10, -10, -20,
        -10, 0, 0, 0, 0, 0, 0, -10,
        -10, 0, 5, 10, 10, 5, 0, -10,
        -10, 5, 5, 10, 10, 5, 5, -10,
        -10, 0, 10, 10, 10, 10, 0, -10,
        -10, 10, 10, 10, 10, 10, 10, -10,
        -10, 5, 0, 0, 0, 0, 5, -10,
        -20, -10, -10, -10, -10, -10, -10, -20};

    int rooks[64] = {
        0, 0, 0, 0, 0, 0, 0, 0,
        5, 10, 10, 10, 10, 10, 10, 5,
        -5, 0, 0, 0, 0, 0, 0, -5,
        -5, 0, 0, 0, 0, 0, 0, -5,
        -5, 0, 0, 0, 0, 0, 0, -5,
        -5, 0, 0, 0, 0, 0, 0, -5,
        -5, 0, 0, 0, 0, 0, 0, -5,
        0, 0, 0, 5, 5, 0, 0, 0};

    int queens[64] = {
        -20, -10, -10, -5, -5, -10, -10, -20,
        -10, 0, 0, 0, 0, 0, 0, -10,
        -10, 0, 5, 5, 5, 5, 0, -10,
        -5, 0, 5, 5, 5, 5, 0, -5,
        0, 0, 5, 5, 5, 5, 0, -5,
        -10, 5, 5, 5, 5, 5, 0, -10,
        -10, 0, 5, 0, 0, 0, 0, -10,
        -20, -10, -10, -5, -5, -10, -10, -20};

    int king_mg[64] = {
        -30, -40, -40, -50, -50, -40, -40, -30,
        -30, -40, -40, -50, -50, -40, -40, -30,
        -30, -40, -40, -50, -50, -40, -40, -30,
        -30, -40, -40, -50, -50, -40, -40, -30,
        -20, -30, -30, -40, -40, -30, -30, -20,
        -10, -20, -20, -20, -20, -20, -20, -10,
        20, 20, 0, 0, 0, 0, 20, 20,
        20, 30, 10, 0, 0, 10, 30, 20};

    int king_eg[64] = {
        -50, -40, -30, -20, -20, -30, -40, -50,
        -30, -20, -10, 0, 0, -10, -20, -30,
        -30, -10, 20, 30, 30, 20, -10, -30,
        -30, -10, 30, 40, 40, 30, -10, -30,
        -30, -10, 30, 40, 40, 30, -10, -30,
        -30, -10, 20, 30, 30, 20, -10, -30,
        -30, -30, 0, 0, 0, 0, -30, -30,
        -50, -30, -30, -30, -30, -30, -30, -50};

public:
    Engine() = default;
    void give_move(Position pos, int depth);
    int negamax(int alpha, int beta, Position pos, int depth);
    int quiesce(int alpha, int beta, Position pos, int depth);
    int evaluate(Position pos);
    int evaluate_piece_tables(Position pos);
    int evaluate_pawns_piece_tables(Position pos);
    int evaluate_knights_piece_tables(Position pos);
    int evaluate_bishops_piece_tables(Position pos);
    int evaluate_rooks_piece_tables(Position pos);
    int evaluate_queens_piece_tables(Position pos);
    int evaluate_kings_piece_tables(Position pos);

    void run();

    void set_position(std::istringstream &is);
    void go(std::istringstream &is);
    void new_game();
    void initialize();
    void quit();
};
#endif // ENGINE_H